import { AdminPanel } from "@/components/admin/AdminPanel";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Админка | CASHER Admin",
};

export default function AdminPage() {
  return <AdminPanel />;
}
